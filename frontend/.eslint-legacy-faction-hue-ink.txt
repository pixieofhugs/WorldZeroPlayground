# Files still painting the bare faction spine hue as an INK (issue #2077).
# This list only ever shrinks — migrating a file means deleting its line here,
# not adding one. New files may never be added to it.
#
# Seeded from what `local/no-faction-hue-as-ink` reported on the day it landed:
# 4 files, 11 violations. The count after each path is that day's number, kept
# as a rough size signal — it is not enforced, because the rule is switched off
# per FILE and a per-line count would rot on the first unrelated edit.
#
# WHY THESE ARE A LIST AND NOT A FIX. #2077's own twelve sites are fixed in the
# same PR; every entry below is a surface that issue does not name, found by the
# rule rather than by the report — which is the rule doing its job. Each is
# NEUTRAL chrome painting a faction name or accent line in the bare hue, i.e.
# the same defect as the twelve, and each is also a visible repaint of a page
# #2077 did not rule on. Two of them argue for themselves in prose:
# `DefaultProposeTask` says at its preview caption that "the faction accent
# stays — ink is measured separately", and `roomPresence` had to be exempted
# outright (see `eslint.config.js`) because its `color` is a y-codemirror data
# field and not a CSS declaration at all. Repainting four pages on the strength
# of a rule that has existed for one commit is the one-at-a-time failure
# WORLD_ZERO_STYLE §3 names from the other side; the numbers are recorded here
# so the follow-up starts with a measurement instead of a search.
#
# THE MEASUREMENT, once, for all of them. Every site below inks on the app's own
# page ground or on `--color-bg-surface` / `-surface-alt` composited over it. In
# LIGHT the bare hue reads (page / frost / surface-alt):
#
#   ephemerists  2.19 / 2.34 / 2.05     snide        2.47 / 2.65 / 2.32
#   coven        2.87 / 3.07 / 2.70     ua           4.46 / 4.77 / 4.19
#   singularity  4.71 / 5.04 / 4.42     default(na)  4.82 / 5.16 / 4.53
#   everymen     5.32 / 5.69 / 5.00     wow          5.80 / 6.20 / 5.44
#
# In DARK every one of the eight clears, 4.62 to 15.35. That is the whole shape
# of this defect class: it sorts perfectly by cascade, so it is one bug and not
# eight, and a dark-only check sees none of it (§3, #1932).
#
# NOT a "pick a darker hue" list. The ceiling is arithmetic: clearing 4.5:1 on
# the frost needs an ink of relative luminance <= 0.1776 and the Ephemerists
# brass sits at 0.3877, so a passing colour on its own hue line is a dark bronze
# and `--faction-ephemerists-on-fill` would stop clearing on the fill it was
# measured for. The hue keeps the fills, washes, rules, rings and glows; the
# type takes a text tier.

src/components/ActivityTicker.tsx  # 2
src/components/InvitationLetterPopup.tsx  # 2
src/components/feed/FeedCardInvitationLetter.tsx  # 3
src/pages/proposeTask/archetypes/DefaultProposeTask.tsx  # 4
