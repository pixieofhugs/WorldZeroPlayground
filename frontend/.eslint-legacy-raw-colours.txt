# Files not yet migrated off raw colour (issue #1853). This list only ever
# shrinks — migrating a file means deleting its line here, not adding one. New
# files may never be added to it.
#
# Seeded from what `local/no-raw-colour-values` actually reported on the day it
# landed: 61 files, 140 violations. #1609 burns it down. The three groups below
# are how the 140 read, not three different rules — every line is one file.
#
# GROUP 1 — the danger banner. CLEARED by #1609: 41 violations across 23 files,
# all `text-red-600` / `border-red-300` / a hardcoded `#dc2626`. They now read
# `.danger-text` / `.danger-edge` / `.warning-text` — three role classes in
# index.css @layer components, one per token, NOT one banner component (that is
# a .tsx decision about type and spacing, and freezing an archetype's metrics in
# the stylesheet the week before the component arrives is the wrong tier).
# `text-red-600` is #dc2626 exactly, so the light render did not move; only dark
# was ever wrong. The eight length counters went to `--color-warning` rather
# than danger — approaching a limit is not an error — which also lifts them off
# the 4.40:1 the red measured on the page.
# `FieldDesk.tsx` was in this group and stayed on the list for a GROUP 2
# violation (its page dot-field wrote its own `rgba(0,0,0,0.035)`); #1609's
# group-2 pass cleared that too, so it is gone from here entirely.
#
# STANDING AT THE END OF GROUP 2: 24 files, 72 violations. Groups 1 and 2 are
# done bar six files parked for a scheduling collision; group 3 is untouched.
#
# GROUP 2 — hand-rolled lift. Neutral chrome carrying its own
# `rgba(0,0,0,α)` shadow or modal scrim at a geometry nobody else uses. This is
# the class #1851 fixed twice already, at two different geometries; a third
# (`AlbescentInvitation`) is here. Wants a shared elevation token, not 19 edits.
#
# MOSTLY CLEARED by #1609: 23 violations across 14 files. The shared elevation
# token is `--color-cast-shadow` (+ `-soft` for an ambient paper lift), a
# COLOUR — geometry stays at the call site, because blur and offset are a
# component's business and only the paint was off-theme. It is deliberately not
# `--color-overlay-strong`: a scrim reads correctly on a near-black page
# unchanged while a cast shadow disappears into it, so only the cast gets a
# dark counterpart (0.25 -> 0.7, tracking --dropdown-shadow's lift). Four sites
# that had already reached for --color-overlay-strong AS a shadow moved too —
# they were the same defect laundered through a token, invisible to this rule.
# The seven modal scrims flattened to `var(--color-overlay-strong)` outright,
# per the ruling `.filter-factions__scrim` already carries in index.css: "one
# scrim for every overlay". Three non-elevation strays went with them —
# CollabRoster's warn veil now mixes the banner's OWN faction ink instead of a
# global amber-500, FieldDesk's dot field mixes `--color-text-primary` so it
# survives dark mode, and FeedCardEraAnnouncement mixes `--badge-admin-text`.
#
# The six below are the same shape and are LEFT ONLY to avoid colliding with
# concurrent card work — four cast shadows (`0 3px 14px rgba(34,26,18,0.1)`,
# `0 2px 6px` the same, `0 12px 32px -14px rgba(0,0,0,0.4)`,
# `0 16px 40px -24px rgba(0,0,0,0.5)`) that `--color-cast-shadow`/`-soft` now
# answer directly, plus two that are not lift at all and want their own call:
# `shared.tsx`'s `1px dashed rgba(128,128,128,0.3)` divider and `TaskCard`'s
# `textShadow: 0 1px 2px rgba(0,0,0,0.3)`.
src/components/praxisCard/desktop/DefaultPraxisCard.tsx
src/components/praxisCard/scoreStamp/DefaultScoreStamp.tsx
src/components/praxisCard/shared.tsx
src/components/taskCard/DefaultTaskCard.tsx
src/components/taskCard/TaskCard.tsx
src/pages/editPraxis/archetypes/DefaultEditPraxis.tsx
#
# GROUP 3 — faction skins and ornament marks. READ THESE BEFORE REPOINTING.
# Gap B's judgement applies to colour exactly as it does to `text-[13px]`: a
# `--faction-*` token names a TIER, and some of these literals name a ONE-OFF
# ORNAMENT instead — Snide's `rgba(182,255,46,0.055)` ghost lettering, the
# halftone dot fields, the offset flat drop shadows that are the print
# metaphor rather than elevation. Migrating those mechanically would push
# ornament colour onto a tier it was never part of, which is the coupling
# WORLD_ZERO_STYLE.md §4a forbids. #1609 must decide per site, and a site that
# stays raw stays on THIS list with the reason written down — never behind an
# inline eslint-disable, which hides the decision from the next reader.
src/components/factionCard/FactionCard.tsx
src/components/factionHero/SingularityFactionHero.tsx
src/components/factionHero/SnideFactionHero.tsx
src/components/factionMarks/TaskCrown.tsx
src/components/metataskSeal/skins/EphemeristsSeal.tsx
src/components/metataskSeal/skins/SnideSeal.tsx
src/components/praxisCard/desktop/EverymenPraxisCard.tsx
src/components/praxisCard/desktop/SingularityPraxisCard.tsx
src/components/selectCard/FactionSelectCard.tsx
src/components/vote/CovenVote.tsx
src/components/vote/EphemeristsVote.tsx
src/components/vote/SnideVote.tsx
src/pages/characterProfile/archetypes/EverymenProfileBody.tsx
src/pages/characterProfile/archetypes/SingularityProfileBody.tsx
src/pages/characterProfile/archetypes/SnideProfileBody.tsx
src/pages/factionDetail/archetypes/EverymenFactionBody.tsx
src/pages/factionDetail/archetypes/SnideFactionBody.tsx
src/pages/fieldDesk/mobileArchetypes/SnideFieldDesk.tsx
